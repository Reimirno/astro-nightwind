---
import NightwindStatus from 'src/components/NightwindStatus';
import Layout from '../layouts/Layout.astro';
import NightwindSwitch from 'src/components/NightwindSwitch';

const title = 'Astro with Nightwind';
---

<Layout title={title}>
	<main class="flex flex-col text-center gap-4 p-16">
		<h1 class="text-4xl font-bold text-center">{title}</h1>
		<p>This is the Astro with Nightwind demo.</p>
		<p>Now, the website theme is: <NightwindStatus client:only="react"/>.</p>
		<p>Try to toggle the theme!</p>
		<div class="flex justify-center">
			<NightwindSwitch client:only="react" />
		</div>
		<p class="mt-8">You should also try reloading the website and see the state persists without flickering!</p>
	</main>
</Layout>
